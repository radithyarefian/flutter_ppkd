import 'package:flutter/material.dart';

class HalamanUtamaSeragam extends StatelessWidget {
  const HalamanUtamaSeragam({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
    body: Column(
      children: [
        Center(
        child: Text("Perlengkapan Sekolah"),
        )
      ],
    ),
    );
  }
}